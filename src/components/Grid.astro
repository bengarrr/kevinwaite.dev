---
interface Props {
    cols: number;
}

const { cols } = Astro.props;
const grid_template = `md:grid-cols-${cols}`;
---

<div class:list={["grid", grid_template, "gap-[10px]"]}>
    <slot></slot>
</div>
<div class="md:grid-cols-2 hidden"></div>